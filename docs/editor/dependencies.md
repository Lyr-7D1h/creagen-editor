# Dependencies

Creagen Editor dependencies are version pinned.

When you add a dependency in the **Menu -> Dependencies** tab, the selected version is stored as an exact semantic version (for example, `0.0.24`), not a floating range like `^0.0.24`.
